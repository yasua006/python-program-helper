[project]
name = "extend-init"
version = "0.6.1"
description = "A Python program that gives you a customized sane template to start with."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
