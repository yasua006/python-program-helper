[project]
name = "extend-init"
version = "0.6.1"
description = "A Python program that gives you a customized sane template to start with."
readme = "README.md"
authors = [
  { name="yasua006" }
]
requires-python = ">=3.14"
dependencies = [
    "build>=1.5.0",
    "prompt-toolkit==3.0.52",
    "questionary==2.1.1",
    "twine>=6.2.0",
    "wcwidth==0.7.0",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.scripts]
extend-init = "extend_init.main:main"

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project.urls]
Homepage = "https://github.com/yasua006/extend-init"
Issues = "https://github.com/yasua006/extend-init/issues"
